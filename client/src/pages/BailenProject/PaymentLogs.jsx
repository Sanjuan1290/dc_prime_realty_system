import { useState } from 'react'
import { useQuery } from '@tanstack/react-query'
import { FiActivity, FiSearch } from 'react-icons/fi'
import PageHeader from '../../components/Shared/PageHeader'
import StatusAlert from '../../components/Shared/StatusAlert'
import { useFetch } from '../../utils/useFetch'
import { formatMoney } from '../../utils/formatMoney'

const PaymentLogs = () => { const [search,setSearch]=useState(''); const {data,isLoading,isError,error}=useQuery({queryKey:['bailen-payment-logs',search],queryFn:()=>useFetch(`/bailen/payment-logs?search=${encodeURIComponent(search)}`)}); const logs=data?.data||[]; const summary=data?.summary||{}; return <main className="flex flex-col gap-6"><PageHeader title="Bailen Payments Audit / Logs" description="Read-only payment history from Bailen listing SOA records." icon={FiActivity} /><StatusAlert type="warning" message="Payments are encoded from Listings / Units → Details → Payments & SOA or from the Payments page. Audit logs are read-only." />{isLoading?<StatusAlert type="loading" message="Loading payment logs..." />:null}{isError?<StatusAlert type="error" message={error?.message||'Failed to load payment logs.'} />:null}<section className="grid gap-4 md:grid-cols-3"><div className="rounded-2xl border bg-white p-5"><p className="text-sm font-bold text-slate-500">Verified Collections</p><h3 className="mt-2 text-2xl font-black">{formatMoney(summary.verified_collections)}</h3></div><div className="rounded-2xl border bg-white p-5"><p className="text-sm font-bold text-slate-500">Audit Log Count</p><h3 className="mt-2 text-2xl font-black">{summary.log_count||0}</h3></div><div className="rounded-2xl border bg-white p-5"><p className="text-sm font-bold text-slate-500">Source</p><h3 className="mt-2 text-2xl font-black">Listings</h3></div></section><section className="rounded-2xl border bg-white shadow-sm"><div className="flex flex-col gap-4 border-b p-4 md:flex-row md:items-center md:justify-between"><div><h2 className="text-lg font-bold">Payment Audit Logs</h2><p className="text-sm font-semibold text-slate-500">Payment status is removed because admin-entered payments are automatically verified.</p></div><label className="relative"><FiSearch className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-400" /><input value={search} onChange={(e)=>setSearch(e.target.value)} placeholder="Search client, unit, reference, or type" className="h-11 rounded-xl border pl-10 pr-3 text-sm font-semibold outline-none focus:border-blue-300 focus:ring-4 focus:ring-blue-50" /></label></div><div className="overflow-x-auto"><table className="w-full min-w-[900px] text-left text-sm"><thead className="bg-slate-50 text-xs font-black uppercase text-slate-500"><tr><th className="px-4 py-3">Date</th><th className="px-4 py-3">Client</th><th className="px-4 py-3">Unit</th><th className="px-4 py-3">Type</th><th className="px-4 py-3">Method</th><th className="px-4 py-3">Amount</th><th className="px-4 py-3">Reference</th><th className="px-4 py-3">Encoded By</th></tr></thead><tbody className="divide-y">{logs.map(p=><tr key={p.payment_id}><td className="px-4 py-4 font-bold">{p.payment_date}</td><td className="px-4 py-4">{p.buyer_name}</td><td className="px-4 py-4">{p.unit_code}</td><td className="px-4 py-4 capitalize">{p.payment_type?.replaceAll('_',' ')}</td><td className="px-4 py-4 capitalize">{p.payment_method?.replaceAll('_',' ')}</td><td className="px-4 py-4 font-bold text-blue-700">{formatMoney(p.amount)}</td><td className="px-4 py-4">{p.reference_id}</td><td className="px-4 py-4">{p.encoded_by_name||'-'}</td></tr>)}</tbody></table></div></section></main> }
export default PaymentLogs
