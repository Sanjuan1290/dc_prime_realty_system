import { useState } from 'react'
import { FiCheckCircle, FiTrash2, FiUploadCloud } from 'react-icons/fi'
import UploadDocumentModal from './UploadDocumentModal'
const Documents = ({ documents = [] }) => { const [rows,setRows]=useState(documents); const [uploadDoc,setUploadDoc]=useState(null); return <section className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="text-xl font-black text-slate-950">Documents</h2><p className="text-sm font-semibold text-slate-500">Mock checklist with upload and delete states.</p></div><span className="rounded-full bg-blue-50 px-3 py-1 text-sm font-black text-blue-700">{rows.filter(r=>r.status==='Approved').length} approved</span></div><div className="mt-5 overflow-x-auto rounded-2xl border border-slate-200"><table className="min-w-full divide-y divide-slate-200 text-sm"><thead className="bg-slate-50"><tr>{['Document','Requirement','Status','File','Actions'].map(h=><th key={h} className="px-4 py-3 text-left text-xs font-black uppercase text-slate-500">{h}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{rows.map(row=><tr key={row.id}><td className="px-4 py-3 font-black">{row.name}</td><td className="px-4 py-3">{row.requirement}</td><td className="px-4 py-3"><span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-black text-slate-700">{row.status}</span></td><td className="px-4 py-3">{row.fileName}</td><td className="px-4 py-3"><div className="flex gap-2"><button onClick={()=>setUploadDoc(row)} className="inline-flex h-9 items-center gap-2 rounded-xl border border-slate-200 px-3 text-xs font-black"><FiUploadCloud />Upload</button><button onClick={()=>setRows(c=>c.map(d=>d.id===row.id?{...d,status:'Missing',fileName:'-'}:d))} className="inline-flex h-9 items-center gap-2 rounded-xl border border-red-200 bg-red-50 px-3 text-xs font-black text-red-700"><FiTrash2 />Clear</button></div></td></tr>)}</tbody></table></div>{uploadDoc ? <UploadDocumentModal document={uploadDoc} onClose={()=>setUploadDoc(null)} onSave={(fileName)=>{setRows(c=>c.map(d=>d.id===uploadDoc.id?{...d,status:'Submitted',fileName}:d)); setUploadDoc(null)}} /> : null}</section> }
export default Documents
