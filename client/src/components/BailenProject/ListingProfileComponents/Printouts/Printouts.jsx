import { useState } from 'react'
import { FiFileText, FiPrinter } from 'react-icons/fi'
import PrintPreviewModal from './PrintPreviewModal'
const Printouts = ({ listing, client, soaRows }) => { const [preview,setPreview]=useState(null); const items=[{title:'Offer to Buy',desc:'Buyer offer form with unit, TCP, reservation, and terms.'},{title:'Buyer Profile',desc:'Buyer information sheet for records.'},{title:'Statement of Account',desc:'SOA schedule with balances and references.'}]; return <section className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><h2 className="text-xl font-black text-slate-950">Printouts</h2><p className="text-sm font-semibold text-slate-500">Preview printable mock forms.</p><div className="mt-5 grid gap-4 md:grid-cols-3">{items.map(item=><button key={item.title} onClick={()=>setPreview(item.title)} className="rounded-3xl border border-slate-200 bg-slate-50 p-5 text-left transition hover:border-blue-200 hover:bg-blue-50"><FiFileText className="h-8 w-8 text-blue-600"/><p className="mt-4 font-black text-slate-950">{item.title}</p><p className="mt-1 text-sm font-semibold text-slate-500">{item.desc}</p><span className="mt-4 inline-flex items-center gap-2 text-sm font-black text-blue-700"><FiPrinter />Preview</span></button>)}</div>{preview ? <PrintPreviewModal title={preview} listing={listing} client={client} soaRows={soaRows} onClose={()=>setPreview(null)} /> : null}</section> }
export default Printouts
