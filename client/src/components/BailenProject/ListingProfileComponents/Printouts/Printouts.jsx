import { useState } from 'react'
import { FiFileText, FiPrinter } from 'react-icons/fi'
import PrintPreviewModal from './PrintPreviewModal'
const Printouts = ({ listing, client }) => { const [preview,setPreview]=useState(null); const items=['Offer to Buy','Buyer Profile','Statement of Account']; return <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="text-lg font-bold text-slate-950">Printouts</h2><p className="text-sm font-semibold text-slate-500">Prepare printable forms for this buyer and unit.</p><div className="mt-5 grid gap-4 md:grid-cols-3">{items.map(item=><button key={item} onClick={()=>setPreview(item)} className="rounded-2xl border border-slate-200 bg-slate-50 p-5 text-left transition hover:border-blue-200 hover:bg-blue-50"><FiFileText className="h-8 w-8 text-blue-600" /><p className="mt-4 font-bold text-slate-950">{item}</p><p className="mt-1 text-sm font-semibold text-slate-500">Open preview then print.</p><span className="mt-4 inline-flex items-center gap-2 text-sm font-bold text-blue-700"><FiPrinter />Preview</span></button>)}</div>{preview ? <PrintPreviewModal title={preview} listing={listing} client={client} onClose={()=>setPreview(null)} /> : null}</section> }
export default Printouts
