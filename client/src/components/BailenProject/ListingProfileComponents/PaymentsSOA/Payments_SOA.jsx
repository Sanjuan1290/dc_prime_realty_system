import { useState } from 'react'
import { FiPlus } from 'react-icons/fi'
import AddPaymentModal from '../../PaymentComponents/AddPaymentModal/AddPaymentModal'
const money = (value) => new Intl.NumberFormat('en-PH', { style: 'currency', currency: 'PHP' }).format(Number(value || 0))
const PaymentsSOA = ({ listing, soaRows = [] }) => { const [rows,setRows]=useState(soaRows); const [showAdd,setShowAdd]=useState(false); const totalDue=rows.reduce((sum,row)=>sum+Number(row.dueAmount||0),0); const totalPaid=rows.reduce((sum,row)=>sum+Number(row.amountPaid||0),0); return <section className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="text-xl font-black text-slate-950">Payments & SOA</h2><p className="text-sm font-semibold text-slate-500">Monthly stays fixed; extra payments shorten duration in mock display.</p></div><button onClick={()=>setShowAdd(true)} className="inline-flex h-10 items-center gap-2 rounded-2xl bg-blue-600 px-4 text-sm font-black text-white"><FiPlus />Add Payment</button></div><div className="mt-5 grid gap-4 md:grid-cols-3"><div className="rounded-2xl bg-slate-50 p-4"><p className="text-xs font-black uppercase text-slate-400">TCP</p><p className="mt-1 text-xl font-black">{money(listing.tcp)}</p></div><div className="rounded-2xl bg-slate-50 p-4"><p className="text-xs font-black uppercase text-slate-400">Total Due</p><p className="mt-1 text-xl font-black">{money(totalDue)}</p></div><div className="rounded-2xl bg-emerald-50 p-4"><p className="text-xs font-black uppercase text-emerald-600">Paid</p><p className="mt-1 text-xl font-black text-emerald-700">{money(totalPaid)}</p></div></div><div className="mt-5 overflow-x-auto rounded-2xl border border-slate-200"><table className="min-w-[1000px] divide-y divide-slate-200 text-sm"><thead className="bg-slate-50"><tr>{['Due Date','Description','Beginning Balance','Due Amount','Date Paid','Amount Paid','Reference ID','Status','Ending Balance'].map(h=><th key={h} className="px-4 py-3 text-left text-xs font-black uppercase text-slate-500">{h}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{rows.map((row,index)=><tr key={index} className="hover:bg-slate-50"><td className="px-4 py-3 font-semibold">{row.dueDate}</td><td className="px-4 py-3 font-black">{row.description}</td><td className="px-4 py-3">{money(row.beginningBalance)}</td><td className="px-4 py-3 font-black">{money(row.dueAmount)}</td><td className="px-4 py-3">{row.datePaid}</td><td className="px-4 py-3 font-black">{money(row.amountPaid)}</td><td className="px-4 py-3">{row.referenceId}</td><td className="px-4 py-3"><span className="rounded-full bg-blue-50 px-3 py-1 text-xs font-black text-blue-700">{row.status}</span></td><td className="px-4 py-3 font-black">{money(row.endingBalance)}</td></tr>)}</tbody></table></div>{showAdd ? <AddPaymentModal listing={listing} onClose={()=>setShowAdd(false)} onSave={(payment)=>{setRows(current=>[...current,payment]); setShowAdd(false)}} /> : null}</section> }
export default PaymentsSOA
