import { useState } from 'react'
import { FiEdit3, FiUser } from 'react-icons/fi'
import EditClientProfileModal from './EditClientProfileModal'
const Detail = ({ label, value }) => <div className="rounded-2xl bg-slate-50 p-4"><p className="text-xs font-black uppercase text-slate-400">{label}</p><p className="mt-1 font-black text-slate-950">{value || '-'}</p></div>
const ClientProfile = ({ client }) => { const [showEdit, setShowEdit] = useState(false); return <section className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="flex items-center gap-2 text-xl font-black text-slate-950"><FiUser />Client Profile</h2><p className="text-sm font-semibold text-slate-500">Mock buyer details and work information.</p></div><button onClick={() => setShowEdit(true)} className="inline-flex h-10 items-center justify-center gap-2 rounded-2xl bg-blue-600 px-4 text-sm font-black text-white"><FiEdit3 />Edit</button></div><div className="mt-5 grid gap-4 md:grid-cols-2 xl:grid-cols-3"><Detail label="Buyer Type" value={client.buyerType} /><Detail label="Buyer Name" value={client.buyerName} /><Detail label="Contact No." value={client.contactNo} /><Detail label="Email" value={client.email} /><Detail label="Address" value={client.address} /><Detail label="Seller" value={client.seller} /><Detail label="Occupation" value={client.occupation} /><Detail label="Employer / Business" value={client.employerBusinessName} /><Detail label="Profile Status" value={client.profileStatus} /></div>{showEdit ? <EditClientProfileModal client={client} onClose={() => setShowEdit(false)} /> : null}</section> }
export default ClientProfile
