# Public asset note

The uploaded source bundle referenced these files by remote URL instead of embedding their binary contents:

- `logo.png`
- `logo-mobile.png`
- `docImage1.png`

Valid placeholder PNG files are included so the project builds and runs. Replace them with the original brand assets when available.


